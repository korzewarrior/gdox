location = /download/auto {
    add_header Cache-Control "private, no-store" always;
    add_header Vary "User-Agent" always;
    add_header X-Robots-Tag "noindex" always;
    return 302 $gdox_download_path;
}

location = /download/auto/ {
    add_header Cache-Control "private, no-store" always;
    add_header Vary "User-Agent" always;
    add_header X-Robots-Tag "noindex" always;
    return 302 $gdox_download_path;
}
